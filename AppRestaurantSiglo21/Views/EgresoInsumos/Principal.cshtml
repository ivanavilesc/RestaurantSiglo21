@model IEnumerable<AppRestaurantSiglo21.Models.DropDownList>

@{
    ViewBag.Title = "Index";
    Layout = Session["Layout"].ToString();
}
<div class="container">
    <div class="card text-center">
        <div class="card-body">
            <h4>Detalle Stock Bodega</h4>
        </div>
    </div>
    <br />
    <table class="table" style="font-family: Helvetica Neue, Helvetica, Arial, sans-serif">
        <tr>
            <th>Código Tipo Insumo</th>
            <th>Tipo Insumo</th>
        </tr>
        @foreach (var ins in Model)
        {
            <tr>
                <td>@Html.DisplayFor(x => ins.Id)</td>
                <td>@Html.DisplayFor(x => ins.Descripcion)</td>
                <td>@Html.ActionLink("Detalle Insumo", "Index", new { idTipoInsumo = ins.Id }, new { @class = "btn btn-warning" })</td>
            </tr>
        }
    </table>
    <div> @Html.ActionLink("Volver atrás", "../Bodega/Index", null, new { @class = "btn btn-secondary" })</div>
</div>