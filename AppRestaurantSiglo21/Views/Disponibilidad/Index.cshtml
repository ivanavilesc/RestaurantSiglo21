@model IEnumerable<AppRestaurantSiglo21.Models.ESTADOMESA>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_PrincipalAdmin.cshtml";
}

<h2>Listado de Disponibilidad</h2>

<p>
    @Html.ActionLink("Crear Registro", "Create")
</p>
<table class="table">
    <tr>
        <th>
            ID de Mesa
        </th>
        <th>
            Estado de Mesa
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IDESTADOMESA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DESCESTADOMESA)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDESTADOMESA }) |
            @Html.ActionLink("Detail", "Detail", new { id = item.IDESTADOMESA }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.IDESTADOMESA })
        </td>
    </tr>
}

</table>
